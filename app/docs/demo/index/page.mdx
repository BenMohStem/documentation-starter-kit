# What runs today

<SceneHeader scene="demo" height={230} />

<PageHead overline="The demo" title="The demo" />

A from-scratch C engine — physics, a first-person 3D renderer, sound,
input, deterministic replay — and a playable demonstration on top.

<Reveal delay={40}>

<GlyphCards cards={[
  { glyph: "bear", title: "Real physics, live", sub: "Bodies with mass, friction, contact — stepping every frame in front of you." },
  { glyph: "wanderer", title: "First person, real input", sub: "Move, look, jump. The world responds because it is simulated, not staged." },
  { glyph: "machine", title: "Deterministic replay", sub: "Same inputs, same world, every time. The engine keeps this promise on every release." },
]} />

</Reveal>

## The demo arc

The demo tells one story, the same story every time, because it is
simulated rather than staged:

1. A region with terrain, light, and weather of its own;
2. A settlement of bodies and simple mechanisms that work — stacked,
   pushed, linked, weighed;
3. A player who can step away and come back to a place that kept
   running.

The arc's last beat is the project's founding promise made
checkable: *the world did not pause while the player was gone.*

## What the demo proves, every release

<FactGrid facts={[
  { number: "full battery", label: "Oracles, negative controls, sanitizers, fresh-process repetition — all green, or there is no release.", source: "the gates" },
  { number: "replay exact", label: "Recorded inputs replay to identical state, byte for byte, on every build that ships.", source: "determinism gate" },
  { number: "2 layouts", label: "Broadphase layouts toggle live in the demo with a key — the same world, different memory order, deterministic both ways.", source: "era 2.10–2.15" },
]} />

> It is deliberately plain. This is the engine proving itself, not
> the world selling itself. Visual polish comes later — on purpose.

## The proof frames

These are real captures from the running engine — not mockups. The
deterministic replay gate guarantees what you see is what shipped:

<div className="wwa-figures">

<ProofFigure src="/screenshots/wwa_proof_frame1_before_input.png" alt="The engine's world before any player input — the resting scene">
  **Before input.** The world at rest: the scene as the deterministic
  simulation composed it, untouched.
</ProofFigure>

<ProofFigure src="/screenshots/wwa_proof_frame_after_real_wasd_mouse_input.png" alt="The same world after real WASD and mouse input">
  **After real input.** The same world after WASD movement and mouse
  look — the player is one participant inside it, never a camera.
</ProofFigure>

</div>

<div className="wwa-quiet-note">

Not yet in the demo: animals, ecology, building, economy — measured
in isolation, entering one honest piece at a time.

</div>
