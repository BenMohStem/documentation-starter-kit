# Std Layer

<SceneHeader scene="std" height={220} />

<span className="wwa-page-title">The floor</span>

Before the engine, the floor: memory, strings, numbers, files,
threads — written by hand, tested adversarially, inherited from
no one.

> An engine that studies memory behavior for a living cannot run on
> a standard library it cannot see inside.

<GlyphCards cards={[
  { glyph: "machine", title: "Designed edge cases", sub: "Identities held over tens of thousands of iterations. Round-trips through every conversion. The strange neighborhoods of floating point." },
  { glyph: "bear", title: "Bugs caught, kept, taught", sub: "Number-parsing and formatting bugs most projects never see — each one kept, with its story. Several are genuinely humbling." },
  { glyph: "machine", title: "Legible, measurable, fixable", sub: "Every allocation, every rounding, every clock tick — yours to see." },
]} />
