# Documentation

<div className="wwa-atmosphere-frame"><Atmosphere variant="ridge" height={170} /></div>

<span className="wwa-page-title">Documentation</span>

Everything about the project, in depth: what it is, how it is planned,
what has actually been done, how every system works, and how the engine
underneath earns the world's trust.

This is the living book of the project. It grows one honest piece at a
time — nothing is written here before it exists or is decided, and what
is still only planned says so plainly.

## Where to start

<div className="wwa-paths">

<Link className="wwa-path" href="/docs/idea">
  <span className="wwa-path-kind">Start here</span>
  <span className="wwa-path-title">The idea</span>
  <span className="wwa-path-sub">What World Without Answers is trying to be — and everything it refuses to be.</span>
</Link>

<Link className="wwa-path" href="/docs/plan">
  <span className="wwa-path-kind">Direction</span>
  <span className="wwa-path-title">The plan</span>
  <span className="wwa-path-sub">Why the engine comes before the world, and the order things are built in.</span>
</Link>

<Link className="wwa-path" href="/docs/steps">
  <span className="wwa-path-kind">History</span>
  <span className="wwa-path-title">Steps & eras</span>
  <span className="wwa-path-sub">What each era of the project actually did — the real log, kept honest.</span>
</Link>

<Link className="wwa-path" href="/docs/systems">
  <span className="wwa-path-kind">Design</span>
  <span className="wwa-path-title">Systems</span>
  <span className="wwa-path-sub">The world's systems — ecology, economy, civilization, war, engineering — documented in depth.</span>
</Link>

<Link className="wwa-path" href="/docs/engine">
  <span className="wwa-path-kind">Foundation</span>
  <span className="wwa-path-title">The engine</span>
  <span className="wwa-path-sub">The from-scratch C foundation under everything: what it is, and why it is built this way.</span>
</Link>

</div>

The measured record behind these pages — every benchmark, experiment,
and failure with its numbers — lives in the
[Research library](/research).
